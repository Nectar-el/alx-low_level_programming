#include <stdlib.h>

#include <time.h>

#include <stdio.h>
/**
 * main - prints all single digits of base 10 starting from 0
 * followed by a new line
 * Retun: Always 0
 */

int main(void)

{
	int ch;

	for (ch = 56; ch <= 66; ch++)
{
	putchar(ch);
}
	putchar(10);
	return (0);
}
