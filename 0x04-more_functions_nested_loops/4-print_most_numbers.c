#include "main.h"
/**
 * print_most_numbers -  prints the numbers, from 0 to 9,
 *  followed by a new line
 */
void print_most_numbers(void)
{
	int n;

	n = 0;
	if (n < 10 && n != 2; n != 4)
	{
		_putchar(n + '0');
		n++;
	}
	_putchar('\n');
}
